---
- hosts: all
  become: true
  tasks:
    - name: Update package repositories
      yum:
        update_cache: yes
    
    - name: Install Nginx package
      yum:
        name: nginx
        state: present
    
    - name: Start nginx service
      service:
        name: nginx
        state: started
        enabled: true

    
